<h1>Listing reports</h1>

<table class="table">
  <thead>
    <tr>
      <th>Förnamn</th>
      <th>Efternamn</th>
      <th>Personnummer</th>
      <th>Antal utredningar</th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @students.each do |student| %>
      <tr>
        <td><%= student.first_name %></td>
        <td><%= student.last_name %></td>
        <td><%= student.socialsecurity %></td>
        <td><%= student.reports.count %></td>
        <td><%= link_to "Skapa utredning", new_report_path(:student_id => student.id), :class => "btn btn-black btn-xs"%></td>
        
      </tr>
      <tr>
  		<ul><% student.reports.each do |r| %>
  			<li><%= link_to r.created_at.strftime('%Y-%m-%d'), report_path(r.id)%></li>
  		</ul>	
  		<% end -%>
    <% end %>
  	  </tr>

  </tbody>
</table>
